# RealTimeChat

Современное приложение для обмена сообщениями в реальном времени, построенное на стеке SvelteKit и NestJS.

## Особенности

- 🚀 **Real-time**: Мгновенный обмен сообщениями через Socket.io.
- 📱 **Responsive**: Адаптивный интерфейс для мобильных и десктопных устройств.
- 🔐 **Auth**: Безопасная аутентификация с использованием JWT (Access & Refresh tokens).
- 📂 **Stories**: Возможность публиковать и просматривать истории (видео).
- 🔄 **Pub/Sub**: Горизонтальное масштабирование веб-сокетов с использованием Redis.

## Стек технологий

- **Frontend**: SvelteKit, Vite, Socket.io-client.
- **Backend**: NestJS, MongoDB (Mongoose), Redis, Socket.io.
- **Shared**: Общие типы и контракты (TypeScript).
- **Инфраструктура**: Docker, Docker Compose.

---

## Запуск через Docker (быстрый старт)

Самый простой способ запустить проект — использовать Docker Compose. Это поднимет все необходимые сервисы: MongoDB, Redis, API, WebSockets и Frontend.

1. Убедитесь, что у вас установлен Docker и Docker Compose.
2. Подготовьте переменные окружения (создайте файлы `.env` на основе примеров):
   ```bash
   cp backend/.env.example backend/.env
   cp frontend/.env.example frontend/.env
   ```
3. Запустите проект:
   ```bash
   docker-compose up --build
   ```
4. После запуска приложение будет доступно по адресам:
   - **Frontend**: [http://localhost:5173](http://localhost:5173)
   - **API (Swagger)**: [http://localhost:3000/api](http://localhost:3000/api)

---

## Локальный запуск (для разработки)

Для полноценной разработки рекомендуется запускать сервисы отдельно.

### Предварительные требования

- Node.js (рекомендуется v22+)
- MongoDB (локально или в Docker)
- Redis (локально или в Docker)

### Шаг 1: Подготовка инфраструктуры

Если у вас нет локально установленных MongoDB и Redis, вы можете запустить их через docker-compose, используя только необходимые сервисы:
```bash
docker-compose up -d mongodb redis
```

### Шаг 2: Настройка Backend

1. Перейдите в папку backend:
   ```bash
   cd backend
   ```
2. Установите зависимости:
   ```bash
   yarn install
   ```
3. Создайте файл `.env` из примера:
   ```bash
   cp .env.example .env
   ```
   *Обязательно проверьте содержимое `.env` и при необходимости настройте подключение к БД и Redis.*
4. Запустите сервер:
   ```bash
   yarn start:dev
   ```

### Шаг 3: Настройка Frontend

1. Перейдите в папку frontend:
   ```bash
   cd ../frontend
   ```
2. Установите зависимости:
   ```bash
   yarn install
   ```
3. Создайте файл `.env` из примера (если он есть):
   ```bash
   cp .env.example .env 2>/dev/null || echo "VITE_API_URL=http://localhost:3000\nVITE_SOCKET_URL=http://localhost:3001" > .env
   ```
4. Запустите приложение:
   ```bash
   yarn dev
   ```

---

## Структура проекта

- `/backend` — NestJS приложение (API и WebSocket шлюз).
- `/frontend` — SvelteKit веб-приложение.
- `/shared` — Библиотека общих типов и DTO (используется и фронтендом, и бэкендом).
- `docker-compose.yml` — Файл конфигурации для развертывания всего стека.

---

## Лицензия

MIT
